# Core do Framework Web
Flask

# Para a segurança (hashing de senhas), que você usa via werkzeug.security
Werkzeug

# Firebase Admin SDK para acesso ao Firestore, Auth, etc.
firebase-admin

# Servidor Web de produção (obrigatório para rodar o Flask no Render)
gunicorn

# ==========================================================
# DEPENDÊNCIAS PARA GERAÇÃO DE PDF (WeasyPrint)
# NOTA: O WeasyPrint requer também a criação do arquivo apt.txt 
#       para as dependências de sistema (Pango, Cairo, etc.)
# ==========================================================
WeasyPrint
cffi
cairocffi 

# Opcional: Se estiver usando o Flask-WeasyPrint para integração mais fácil
# Flask-WeasyPrint
